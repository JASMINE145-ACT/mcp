# MCP Collection

This repository contains standalone MCP servers.

## Servers

- `quotation-mcp/` - Wanding quotation and inventory MCP. Python retrieves quotation candidates and inventory data; Claude Code performs final quotation selection without an extra selector model.
